import { PageHero } from "@/components/page-hero";
import { t } from "@/lib/i18n";
import { getLocale, localizedMetadata } from "@/lib/i18n-server";
export const generateMetadata = () => localizedMetadata({ path: "/privacy", title: "Collector Privacy Contract", zhTitle: "采集器隐私约定", description: "Exactly what LovTokens reads, sends, stores, and never collects.", zhDescription: "明确说明 LovTokens 会读取、发送、存储以及绝不会采集的内容。" });
export default async function PrivacyPage() { const locale = await getLocale(); return <><PageHero eyebrow={t(locale, "Privacy contract")} title={t(locale, "Aggregate the count. Leave the work.")} description={t(locale, "LovTokens has a deliberately narrow data boundary that can be inspected before every sync.")} /><article className="shell prose-shell"><h2>{t(locale, "Known directories only")}</h2><p>{locale === "zh" ? <>采集器只读取 <code>$CODEX_HOME/sessions</code> 和 <code>archived_sessions</code> 下的 Codex 会话，以及已知配置目录中的 Claude Code 项目日志。它不会递归搜索主目录，也不会跟随指向这些根目录之外的符号链接。</> : <>The collector reads Codex sessions under <code>$CODEX_HOME/sessions</code> and <code>archived_sessions</code>, plus Claude Code project logs under known configuration directories. It does not recursively search the home directory and does not follow symlinks outside those roots.</>}</p><h2>{t(locale, "Uploaded fields")}</h2><p>{locale === "zh" ? "数据源、UTC 日期、模型、单向会话指纹、Token 组成、请求数、首次/末次事件时间戳、解析器版本和覆盖率。设备认证与使用量数据相互独立。" : "Source, UTC date, model, a one-way session fingerprint, token components, request count, first/last event timestamps, parser version, and coverage. Device authentication is separate from usage."}</p><h2>{t(locale, "Never uploaded")}</h2><ul>{locale === "zh" ? <><li>提示词、助手回复、工具输出或代码</li><li>文件路径、项目名称、仓库名称或 Git 远程地址</li><li>API Key、服务商 Cookie 或私有服务商端点</li><li>原始日志文件或稳定的设备标识符</li></> : <><li>Prompts, assistant replies, tool output, or code</li><li>File paths, project names, repository names, or Git remotes</li><li>API keys, provider cookies, or private provider endpoints</li><li>Raw log files or stable machine identifiers</li></>}</ul><h2>{t(locale, "Public by choice")}</h2><p>{locale === "zh" ? "新档案默认私密。精确总量、排名、头像、模型和费用均可独立控制。隐藏字段会在服务端从 HTML、JSON-LD、API 结果和生成图片中移除。" : "New profiles are private. Exact totals, rank, avatar, models and cost each have independent controls. Hidden fields are removed on the server from HTML, JSON-LD, API results and generated images."}</p><h2>{t(locale, "Control")}</h2><p>{locale === "zh" ? <>用户可以使用 <code>lovtokens show-data</code> 检查上传内容、撤销设备、导出汇总数据、隐藏档案或删除账号。已删除证书仅保留不含身份信息的撤销证明。</> : <>Users can inspect an upload with <code>lovtokens show-data</code>, revoke devices, export aggregate data, hide a profile, or delete the account. Deleted certificates retain only a revoked proof without identity.</>}</p></article></>; }
