import { PageHero } from "@/components/page-hero";
import { t } from "@/lib/i18n";
import { getLocale, localizedMetadata } from "@/lib/i18n-server";
export const generateMetadata = () => localizedMetadata({ path: "/docs", title: "LovTokens Collector Documentation", zhTitle: "LovTokens 采集器文档", description: "Install, connect, inspect, sync and remove the open LovTokens token collector.", zhDescription: "安装、连接、检查、同步和移除开源 LovTokens Token 采集器。" });
export default async function DocsPage() { const locale = await getLocale(); return <><PageHero eyebrow={t(locale, "Collector docs · Node 20+")} title={t(locale, "One command, visible boundaries.")} description={t(locale, "Connect Codex and Claude Code usage from macOS, Linux, or Windows without uploading the conversations behind the count.")} /><article className="shell prose-shell"><h2>{t(locale, "Connect")}</h2><div className="docs-command"><code>npx lovtokens@latest connect</code></div><p>{t(locale, "A browser opens with a one-time code. Create an account or sign in, approve that code, and the CLI performs its first scan.")}</p><h2>{t(locale, "Inspect before upload")}</h2><pre><code>npx lovtokens@latest show-data</code></pre><p>{t(locale, "This prints the complete JSON payload. It contains schema version, collector version, a random device ID, timestamps, and aggregate usage buckets.")}</p><h2>{t(locale, "Commands")}</h2><table><tbody><tr><td><code>lovtokens sync</code></td><td>{t(locale, "Rescan and replace absolute bucket values idempotently.")}</td></tr><tr><td><code>lovtokens status</code></td><td>{t(locale, "Show sources, coverage, last sync and connection.")}</td></tr><tr><td><code>lovtokens card</code></td><td>{t(locale, "Open the latest public share card.")}</td></tr><tr><td><code>lovtokens auto-sync install</code></td><td>{t(locale, "Opt in to a 30-minute user-level task.")}</td></tr><tr><td><code>lovtokens auto-sync remove</code></td><td>{t(locale, "Remove only the LovTokens task.")}</td></tr><tr><td><code>lovtokens disconnect</code></td><td>{t(locale, "Revoke this device and delete local credentials.")}</td></tr></tbody></table><h2>{t(locale, "Data sources")}</h2><p>{locale === "zh" ? <>Codex：<code>$CODEX_HOME/sessions</code> 和 <code>archived_sessions</code>。Claude Code：已知的 <code>~/.claude/projects</code> 兼容目录。指向这些根目录之外的符号链接会被跳过。</> : <>Codex: <code>$CODEX_HOME/sessions</code> and <code>archived_sessions</code>. Claude Code: known <code>~/.claude/projects</code> compatible directories. Symlinks escaping these roots are skipped.</>}</p><h2>{t(locale, "Credentials")}</h2><p>{locale === "zh" ? <>采集器会优先使用操作系统凭据存储。便携式回退方案是权限限制为 <code>0600</code> 的配置文件。不会请求任何服务商 API Key。</> : <>The collector uses the operating system credential store when available. The portable fallback is a configuration file restricted to mode <code>0600</code>. No provider API key is requested.</>}</p><h2>{t(locale, "Troubleshooting")}</h2><h3>{t(locale, "No events found")}</h3><p>{locale === "zh" ? <>运行 <code>lovtokens status</code>，对比扫描文件数与包含使用量的文件数。缺失事件会显示为覆盖率警告，而不会被估算为零。</> : <>Run <code>lovtokens status</code> and review files scanned versus files with usage. Missing events become a coverage warning, not an estimated zero.</>}</p><h3>{t(locale, "Reset a device")}</h3><p>{locale === "zh" ? <>运行 <code>lovtokens disconnect</code>，然后重新连接。智能体日志绝不会被修改。</> : <>Run <code>lovtokens disconnect</code>, then connect again. Agent logs are never altered.</>}</p></article></>; }
