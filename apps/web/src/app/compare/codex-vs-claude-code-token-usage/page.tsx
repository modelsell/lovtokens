import { PageHero } from "@/components/page-hero";
import { getLocale, localizedMetadata } from "@/lib/i18n-server";
export const generateMetadata = () => localizedMetadata({ path: "/compare/codex-vs-claude-code-token-usage", title: "Codex vs Claude Code Token Usage", zhTitle: "Codex 与 Claude Code Token 使用量对比", description: "A precise comparison of how Codex and Claude Code logs represent input, cache and output token usage.", zhDescription: "精确对比 Codex 与 Claude Code 日志如何表示输入、缓存和输出 Token 使用量。" });
export default async function ComparePage() { const locale = await getLocale(); if (locale === "zh") return <><PageHero eyebrow="对比指南" title="Codex 与 Claude 的统计方式不同。" description="只有统一累计计数器和缓存语义后，二者总量才能进行比较。" /><article className="shell prose-shell"><table><thead><tr><th>维度</th><th>Codex</th><th>Claude Code</th></tr></thead><tbody><tr><td>常见事件形式</td><td>累计 Token 计数快照</td><td>逐消息使用量对象</td></tr><tr><td>输入缓存</td><td>通常包含在输入总量中</td><td>与非缓存输入同时报告</td></tr><tr><td>归一化</td><td>将计数器转换为非负增量</td><td>只把缓存创建/读取加入原生输入一次</td></tr><tr><td>缺失事件</td><td colSpan={2}>标记为部分覆盖，绝不估算</td></tr></tbody></table><h2>为什么原始总量不一致</h2><p>如果某字段已经包含 Claude Code 缓存读取，再次累加会虚增使用量；如果原生字段不含缓存却没有补入，则会少算。Codex 累计快照会造成另一种错误：直接累加每个快照，而不是累加快照之间的增量。</p><h2>LovTokens 的统一口径</h2><pre><code>processed tokens = complete input + output</code></pre><p>推理仍是输出明细，缓存仍是输入明细。这样既能提供可比的使用量视图，也能保留原始组成用于审计。</p><div className="callout">可比较的 Token 总量仍不等于可比较的生产力。智能体行为、任务规模、上下文策略、重试、模型选择和价格都不相同。</div></article></>; return <><PageHero eyebrow="Field guide" title="Codex and Claude count differently." description="The totals can be compared only after cumulative counters and cache semantics are normalized." /><article className="shell prose-shell"><table><thead><tr><th>Dimension</th><th>Codex</th><th>Claude Code</th></tr></thead><tbody><tr><td>Common event shape</td><td>Cumulative token count snapshots</td><td>Per-message usage objects</td></tr><tr><td>Input cache</td><td>Usually included in input total</td><td>Reported alongside uncached input</td></tr><tr><td>Normalization</td><td>Convert counters to non-negative deltas</td><td>Add cache creation/read to native input once</td></tr><tr><td>Missing events</td><td colSpan={2}>Mark partial coverage; never estimate</td></tr></tbody></table><h2>Why raw totals disagree</h2><p>Adding Claude Code cache reads to a field that already includes them inflates usage. Failing to add them when the native field excludes cache undercounts it. Codex cumulative snapshots create a different error: adding each snapshot instead of its delta.</p><h2>LovTokens&apos; common denominator</h2><pre><code>processed tokens = complete input + output</code></pre><p>Reasoning remains an output detail and cache remains an input detail. This supports a comparable usage view while preserving the original components for audits.</p><div className="callout">A comparable token total is still not a comparable productivity score. Agent behavior, task size, context strategy, retries, model choice, and pricing all differ.</div></article></>; }
