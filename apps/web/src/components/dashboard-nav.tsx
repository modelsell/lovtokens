import type { Locale } from "@/lib/i18n";
import { localePath, t } from "@/lib/i18n";
import { LocaleLink } from "./locale-link";
import { SignOutButton } from "./sign-out-button";
export function DashboardNav({ locale }: { locale: Locale }) { const link = (path: string) => localePath(path, locale); return <nav className="dashboard-nav"><LocaleLink href={link("/dashboard")} locale={locale}>{t(locale, "Overview")}</LocaleLink><LocaleLink href={link("/dashboard/share")} locale={locale}>{t(locale, "Share studio")}</LocaleLink><LocaleLink href={link("/dashboard/certificates")} locale={locale}>{t(locale, "Certificates")}</LocaleLink><LocaleLink href={link("/settings/profile")} locale={locale}>{t(locale, "Public profile settings")}</LocaleLink><LocaleLink href={link("/settings/privacy")} locale={locale}>{t(locale, "Privacy and leaderboard")}</LocaleLink><LocaleLink href={link("/settings/devices")} locale={locale}>{t(locale, "Devices")}</LocaleLink><LocaleLink href={link("/settings/account")} locale={locale}>{t(locale, "Account and security")}</LocaleLink><LocaleLink href={link("/settings/data")} locale={locale}>{t(locale, "Data export and deletion")}</LocaleLink><SignOutButton locale={locale} /></nav>; }
